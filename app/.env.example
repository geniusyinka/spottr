# Backend URL the app calls to mint Realtime ephemeral keys.
# For local development, set this to your machine's LAN IP (not localhost — the
# device on your phone can't reach localhost).
EXPO_PUBLIC_BACKEND_URL=http://192.168.1.10:8787
